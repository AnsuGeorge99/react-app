import * as apiPath from '../paths/customers';

const baseUrl = "/api";

const customerClient = 
{
    // async postLoginByAdmin() {
    //     return await client.post(apiPath.postLoginByAdmin);
    // }
    async postLoginByAdmin()
    {

    }
}
export {customerClient};